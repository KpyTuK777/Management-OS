# Living Organizational Model broad Implementation Authority correction supplement

## Control

- Corrects the scope/reconciliation at commit `83f46a02a52f4e8f5d6996814c581d40d341b147`, blob `a7e0669ae3acf817078832842114076398467f8a`
- Corrects the execution contract at commit `fb45b4205306ac48e095dc13d2ed175c9ce66fbe`, blob `53a0b3aca50e0bc73d563ff84d5b7d80629dba91`
- Responds to critical review commit `be7a52a1d301aa7d5538e2e82c2e46003763bdef`, blob `d763b28ccb8086b4accbbfb125dfdc7346862961`
- Effect: these rules control wherever the preparation artifacts are silent or less specific
- Implementation authority: NONE

## C-1 — local container identity is not authentication

The bounded browser materialization may generate one opaque local `workspaceId` solely to partition repository records and detect cross-container references. It is not an Account, Membership, login, credential, authorization proof, sharing token, or human authority. Client possession or modification of it confers no additional capability.

The local runtime remains a single-browser evaluation boundary. Named/restricted people functions stay denied independently of the identifier. Authentication, Membership enforcement, sharing, workspace transfer, multi-user access, and server-enforced authorization are stop conditions.

## C-2 — hard-disabled people runtime

Future-compatibility code may define bounded kinds, interfaces, denial policies, and policy tests required by the adopted architecture. The authorized runtime must not create, persist, import, index, count, navigate to, project, or display named-person, occupancy, membership, subject, or InformationContribution records.

Person Workbench and person search may show only an honest unavailable state without record-derived counts or hints. Tests may use non-human opaque sentinels isolated to test memory/fixtures; they must not be included in production initialization, demonstration content, screenshots presented as user state, or committed browser stores.

## C-3 — Investigation migration acceptance gate

Migration from existing Investigation v1 stores is copy-on-write and cannot activate until a compatibility comparison proves preservation for every saved record of:

- record and active-selection identity;
- title and initial/context description;
- Current Situation and all revisions;
- materials, IDs, source metadata, status, and timestamps;
- Hypotheses, IDs, status, support and contradiction links;
- Working Set membership and ordering where meaningful;
- timeline entries;
- created/updated timestamps and saved-record count.

Every relationship target must resolve after migration. The comparison produces no sensitive payload in logs. Any mismatch, malformed source, capacity failure, interrupted write, or unknown version blocks activation, retains every original key unchanged, and opens compatible original data through the existing reader/workbench where safely possible. Original keys remain untouched for the full authorized cycle; removal requires later separate reviewed authority.

## C-4 — legacy Improvement quarantine boundary

Existing `improvement-proposals.html` data and related scripts predate the adopted Improvement mode and are non-authoritative compatibility input. The executor may reuse generic styling, modal, rendering, or storage utility code only after removing legacy semantic coupling.

No legacy Improvement record may become an adopted Improvement record without a separately reviewed field/state mapping proving semantic equivalence and preservation. This cycle does not authorize such a mapping. Legacy data remains untouched and either accessible through its existing clearly separate route or outside the new navigation; it must not appear as current adopted Improvement state.

## C-5 — changed-file traceability

Implementation evidence must enumerate every changed file and classify it as:

1. new organization-first Product materialization;
2. compatibility/refactor required to integrate preserved capability;
3. test/fixture;
4. execution evidence;
5. implementation review/correction;
6. allowed run-documentation update.

Each entry states one-line necessity and the adopted scope it serves. Unrelated cleanup, files outside permitted areas, authority-artifact mutation, ROADMAP/BACKLOG change, generated profiles, or machine-specific paths fail review. Pre-existing user changes must be identified and preserved rather than absorbed or reverted.

## C-6 — real-entry browser verification

Automated model tests and smoke harnesses supplement but never replace direct browser execution of the real `index.html` application. Evidence must identify browser/version, viewport, storage precondition, journey, result, console errors, uncaught runtime errors, and screenshot/result artifact where applicable.

The real entry must directly verify empty start, organization creation, reload persistence, migration compatibility, Map/Workbench navigation, process modelling, contextual Investigation non-promotion, Improvement current/proposed separation, modal/drawer stress, Historical Walkthrough access, desktop composition, and 390 px behavior. At least one recovery/corruption journey must run against the same production persistence adapter used by `index.html`.

No successful handoff is permitted with unexplained console/runtime errors, a smoke-only result, fabricated screenshot state, or tests against implementation paths not loaded by the real application.

## C-7 — export/import excluded

Export/import is outside the default authorized implementation scope, including people-free export/import. Retained immutable generations, compatibility reads, and deterministic recovery are the bounded safety mechanisms for this cycle.

If execution proves those mechanisms cannot satisfy recovery or experiential-review completion, it must stop with evidence and request a bounded authority revision. It must not add export/import opportunistically.

## Correction disposition

IR-1 through IR-7 are addressed without expanding canonical, Product, Interaction, privacy, or Engineering meaning. The original preparation artifacts, as controlled by this supplement, are ready for repeat review.

## Authority effect

NONE. This supplement corrects preparation artifacts only and authorizes no application, UI, test, dependency, transition, or deployment change.
